[project]
name = "02460_root"
version = "0.1.0"
description = "Root workspace for my Python projects"
dependencies = [
    "torch-geometric>=2.7.0",
] # Keep this empty if the root has no code

[tool.uv.workspace]
members = ["Project1", "Project2", "Project3"]
